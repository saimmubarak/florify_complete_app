# Requirements for YOLO Plant Detection Lambda Container
# Optimized for CPU-only inference (no GPU/CUDA needed)

# Core AWS Lambda dependencies
boto3
botocore

# YOLO and ML dependencies - MINIMAL VERSIONS
# Install opencv-python-headless BEFORE ultralytics to prevent opencv-python installation
opencv-python-headless>=4.5.0

# PyTorch CPU-only (saves ~2GB by excluding CUDA libraries)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu
torchvision==0.17.0+cpu

# YOLO and other dependencies
ultralytics>=8.0.0
Pillow>=9.0.0
numpy<2.0.0

# Authentication
python-jose[cryptography]
PyJWT
